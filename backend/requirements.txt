# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Database
sqlalchemy==2.0.30
asyncpg==0.29.0           # async PostgreSQL driver
alembic==1.13.1           # migrations (optional, using init.sql for simplicity)
psycopg2-binary==2.9.9    # sync driver (used by Alembic)

# Validation / settings
pydantic==2.7.1
pydantic-settings==2.3.0
python-dotenv==1.0.1
email-validator==2.1.1

# HTTP client
httpx==0.27.0             # for integration tests / sample client

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
